"""Organization isolation tests — replaced by test_global_access.py.

The server no longer has organization concepts. API keys are global and all
connections are accessible to any authenticated key. See test_global_access.py
for the replacement tests.
"""
